name: 機能追加
description: 新機能・画面追加などの提案です。ブランチ接頭辞は feature/ を使います。
title: "[Feature]: "
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        機能追加の提案ありがとうございます！以下の項目を埋めてください。
        着手ブランチは `担当分野/feature/機能ID番号`（例: `front/feature/04`）で作成します。
  - type: textarea
    id: summary
    attributes:
      label: 機能概要 / 目的
      description: どんな機能か、なぜ必要かを記載してください。
      placeholder: 例）貸出中一覧に利用者名を表示し、誰が借りているか一目で分かるようにする。
    validations:
      required: true
  - type: dropdown
    id: scope
    attributes:
      label: 対応スコープ
      description: README のスコープ区分を選んでください。
      options:
        - MVP-A（必須）
        - MVP-B（Stretch）
        - スコープ外検討
    validations:
      required: true
  - type: dropdown
    id: area
    attributes:
      label: 担当分野
      description: ブランチ命名の1階層目（front / back）になります。
      options:
        - front
        - back
        - 両方
    validations:
      required: true
  - type: input
    id: feature-id
    attributes:
      label: 関連機能ID
      description: README の機能ID。ブランチ命名の3階層目（番号のみ）になります。
      placeholder: 例）F-04（ブランチ → front/feature/04）
    validations:
      required: false
  - type: textarea
    id: acceptance
    attributes:
      label: 受け入れ条件チェックリスト
      description: この機能が完成したと判断できる条件をチェックリストで記載してください。
      placeholder: |
        - [ ] 貸出中一覧に利用者名が表示される
        - [ ] 返却済みの書籍は一覧に出ない
        - [ ] 一般社員以上のロールで閲覧できる
    validations:
      required: true
  - type: textarea
    id: notes
    attributes:
      label: 補足・参考リンク
      description: 設計資料、デザイン、関連 Issue などがあれば記載してください。
      placeholder: 例）docs/design/screen-transition.md の貸出中一覧画面を参照
    validations:
      required: false
